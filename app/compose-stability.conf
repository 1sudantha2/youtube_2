// Compose compiler stability configuration.
// Every UI-facing model lives here, so all list items are skippable
// and lambdas capturing them stay immutable => zero wasted recompositions.
com.youtubelite.app.model.**
