# You-Tube hand-authored baseline profile.
# Parsed by androidx.profileinstaller at first run (or cloud-compiled at install
# on newer devices) to AOT-compile the hot paths -> faster cold start & playback.
# Wildcard rules keep this maintainable; library AARs (Compose, Media3 UI-less)
# contribute their own profiles automatically.

# App's own UI + engine hot paths
HSPLapp/you/tube/**->**(**)**
Lapp/you/tube/**

# Player engine startup & buffering paths
HSPLandroidx/media3/exoplayer/upstream/**
HSPLandroidx/media3/exoplayer/source/**
HSPLandroidx/media3/exoplayer/audio/**
HSPLandroidx/media3/exoplayer/video/**
HSPLandroidx/media3/common/**
